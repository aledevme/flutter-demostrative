import 'package:flutter/material.dart';

class Explorer extends StatelessWidget {
  const Explorer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('This is explorer page'),
      ),
    );
  }
}